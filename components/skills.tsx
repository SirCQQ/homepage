import React from "react";
import Page from "components/page";

const Skills: React.FunctionComponent<any> = ({ children, ...props }) => {
  return <Page {...props}>Skills</Page>;
};

export default Skills;
