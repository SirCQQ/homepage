import React from "react";
import Page from "components/page";

const Contact: React.FunctionComponent<any> = ({ children, ...props }) => {
  return <Page {...props}>Contact</Page>;
};

export default Contact;
